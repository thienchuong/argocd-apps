apiVersion: v1
kind: ConfigMap
metadata:
  name: networking-cluster
  namespace: observability
  labels:
    grafana_dashboard: "1"
data:
  networking_cluster.json: |
{{ .Files.Get "grafana-dashboards/test.json" | indent 4 }}
